services:
  hours-dashboard-rd:
    build: .
    container_name: hours-dashboard-rd
    network_mode: host
    environment:
      - NODE_ENV=production
      - PORT=5183
      - DATABASE_URL=${DATABASE_URL}
      - GITLAB_URL=${GITLAB_URL}
      - GITLAB_TOKEN=${GITLAB_TOKEN}
      - ALLOWED_DEPTS=AI產品研發部
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:5183/api/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 5
